---
description: Выполнить задачи tasks.md по порядку с проверкой и обновлением графа
argument-hint: <NNN или slug фичи>
---

Реализуй `specs/$ARGUMENTS/` строго по `tasks.md`. Предусловие: `/analyze` без blocker'ов.

Для каждой задачи:

1. Перечитай задачу и трассировку; уточни файлы через `graphify_impact` (не Grep/Glob).
2. Тест раньше реализации (Vitest + @nuxt/test-utils), затем компонент/страница/composable.
3. Стиль окружения: PrimeVue + дизайн-токены (`var(--ehd-*)`, `app/theme.ts`), никаких inline-стилей и сторонних UI-библиотек; сервер-стейт через `useAsyncData`; Pinia только кросс-страничное; фронт не расширяет права.
4. Проверка после задачи: `pnpm typecheck` (без ошибок), релевантные тесты, при необходимости `pnpm build`.
5. «Выполнено» только при наблюдаемом зелёном результате.

После всех задач:
- Визуальная проверка на стеке (`docker compose -f docker-compose.dev.yml`) на 1024/1280/1440; состояния Loading/Empty/Error/Access denied.
- `graphify_build update=true`.
- Сверься с Definition of Done в конституции.

Каждое утверждение о результате подтверждай выводом инструмента. Ничего не выдумывай.
